{
  "name": "ac-agent-presets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AgentChat preview 预设 Agent 目录（src agent-presets 的 preview 落地：内置预设数据 + ctx.agents 物化 + 默认池模型解析）",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@agentchat/cordis": "workspace:*",
    "ac-agents": "workspace:*"
  }
}
