{
  "name": "ac-config-merge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "配置 diff/merge 纯库（零 cordis 依赖）：deepMerge（全局+差异合成有效配置）+ computeDiff（只写差异项）——Agent 差异配置/管理面共用",
  "exports": {
    ".": "./src/index.ts",
    "./src/*": "./src/*"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
