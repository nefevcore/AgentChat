{
  "name": "ac-agent-presets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AgentChat 预设模式目录（ctx.agentPresets 注册中心：数据行注入预设定义 → ctx.agents 物化 + 默认池模型解析；内置数据住 ac-agent-presets-builtin）",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node ../../scripts/run-tests.mjs ."
  },
  "dependencies": {
    "@agentchat/cordis": "workspace:*",
    "ac-llm-pool": "workspace:*"
  },
  "devDependencies": {
    "ac-agents": "workspace:*",
    "ac-config": "workspace:*",
    "ac-extension-core": "workspace:*"
  },
  "agentchat": {
    "plugin": true
  },
  "keywords": [
    "agentchat"
  ]
}
