{
  "name": "ac-subagent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "子 Agent 行：多轮会话实体（spawn/send/await/list/stop/delete）+ 消息与注册表落盘（跨重启续聊）+ send 四投递语义（sync/steer/next-run）+ 每 run job 登记（kind=subagent）",
  "exports": {
    ".": "./src/index.ts",
    "./src/*": "./src/*"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@agentchat/cordis": "workspace:*",
    "ac-tools": "workspace:*",
    "ac-agent-loop": "workspace:*",
    "ac-jobs": "workspace:*",
    "ac-agents": "workspace:*",
    "ac-llm": "workspace:*",
    "ac-llm-pool": "workspace:*"
  },
  "devDependencies": {
    "ac-config": "workspace:*",
    "ac-extension-core": "workspace:*",
    "vitest": "^4.1.10"
  },
  "agentchat": {
    "plugin": true
  },
  "keywords": [
    "agentchat"
  ]
}
