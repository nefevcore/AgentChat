// ============================================================
// client/css-modules.d.ts —— 懒注入样式表的模块声明
//
// useMarkdown 的 KaTeX 懒路径动态 import 两份样式表（vite 侧由
// vite/client 提供 *.css 声明；根 tsc 的 types 只有 node，故本包自备
// 通配声明——仅用于动态样式导入的类型消解，不改变构建行为）。
// ============================================================
declare module '*.css';
