{
  "name": "@agentchat/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent 配置与注册域：AgentConfig 契约 + 三层配置合并 + 注册表 + 凭据加密 —— 迁移自 src/agents/{config,registry,config-diff,credential-store,paths}",
  "exports": {
    ".": "./src/index.ts",
    "./src/*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentchat/agent-config": "workspace:*",
    "@agentchat/agent-loop": "workspace:*",
    "@agentchat/contracts": "workspace:*",
    "@agentchat/cordis": "workspace:*",
    "@agentchat/llm": "workspace:*",
    "@agentchat/types": "workspace:*",
    "@agentchat/util": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
