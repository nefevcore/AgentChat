{
  "name": "ac-relay-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "哑中继服务器：房间制密文帧转发（零业务理解、零持久化、防滥用限额）——remote-client-relay-plan §4.3 的实现",
  "exports": {
    ".": "./src/index.ts",
    "./src/*": "./src/*"
  },
  "scripts": {
    "test": "vitest run",
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --outfile=dist/relay.mjs --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" && esbuild src/download-gate.ts --bundle --platform=node --format=esm --outfile=dist/download-gate.mjs",
    "start": "node dist/relay.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "esbuild": "^0.24.0",
    "vitest": "^4.1.10"
  }
}
