// ====================================================================
// agent-memory types —— 记忆扩展类型定义
// ====================================================================

/** 记忆条目（未来用于结构化记忆时的类型定义） */
export interface MemoryEntry {
  /** 条目唯一标识 */
  id: string;
  /** 条目内容 */
  content: string;
  /** 创建时间 */
  created_at: string;
  /** 最后更新时间 */
  updated_at: string;
}
