// ============================================================
// @agentchat/contracts/src/group-contract.ts —— 群聊行为契约正典
//
// 实测结论（迁移自 router.ts hint 内联文案，v3 机制化为 group-contract 钩子）：
// 契约放系统提示词会因长上下文注意力稀释失效（群聊是最长上下文场景），
// 必须位于"回/不回"决策点（注入单元或历史尾部之后）。文案逐字保留，
// I11 快照测试锚定——修改需过 A/B 验收（docs/group-single-channel-design.md §2.4）。
// ============================================================

/** 群聊行为契约正文（怎么回 / 直接输出无效 / 沉默权 / 不刷屏） */
export const GROUP_CONTRACT_TEXT =
  '收到群聊消息：若值得回应，请调用工具 send_group 把回复发回群聊——直接输出文本不会发送到群聊、其他成员看不到；若无话可说则保持沉默，请注意不要刷屏。';
