// core/session —— 会话状态管理（v0.5.0 P1）
export { SessionManager, convKeyFor } from './session-manager';
export type { RunSession } from './run-session';
export { createRunSession } from './run-session';
