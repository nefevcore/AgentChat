# AgentChat profile root — 空条目表。组合在补丁层完成：
#   bundle: src/boot/boot/src/composition.base.yml
#   ← 用户层: cordis.patch.yml（本目录）
#   ← 机器层: $AGENTCHAT_HOME/cordis.patch.yml
#   ← 覆盖: agentchat dev --patch <file.yml>
# 本文件每次启动都会被重写。请编辑补丁层，不要在此堆行。
[]
